services:
  frontend:
    build: ./frontend
    ports: 
      - "3000:80"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
    environment:
      - NODE_ENV=production
  # database:
  backend:
    build: ./backend
    ports: 
      - "8080:8080"
